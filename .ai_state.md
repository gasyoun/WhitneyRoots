# Project Objective: Whitney-Root Crosswalk (Grammar × Corpus × Dictionaries)

Link grammar (Whitney), corpus (DCS), and dictionaries (Cologne) into one FAIR, citable
dataset keyed on the Whitney root, for a Sanskrit student. Full design: **`DESIGN.md`**.

This **subsumes** the earlier "Whitney Grammar §-Citation" phase: its root→§ work becomes
**Layer 2** (`Whitney_Grammar_Citations.md`, `src/grammar_refs.json`, the `grammar_ref` field,
and `src/wg_text.txt` all feed it), and its human-review queues (below) stay valid.
**Authority order unchanged: Grammar > Roots > corpus > Zalizniak** (corpus is lowest).

---

## ➡️ Next Steps (Queue)

**Crosswalk build (per `DESIGN.md`):**
- [x] **Phase 0 — DONE (2026-06-13).** Parsed the full local warnemyr mirror `1885/` (939 pages) →
      `scratch/phase0/root_spine.json` (930 keyed, 35 `class_uncertain`) + 23-flag `audit.md`.
      **No network re-harvest needed** — `1885/` is the complete mirror (now gitignored, 13 MB).
- [x] **FAIR emitters — DONE.** `scripts/emit_crosswalk.py` → `crosswalk/` (CSV + normalized
      `root_class.csv` + SQLite + Turtle/RDF [rdflib-validated, 11.6k triples] + CSVW). IAST→SLP1 inline.
- [x] **Phase 1 — DONE (2026-06-13).** `scripts/fold_corpus.py` folded DCS freq/forms into the spine
      under a `corpus` key (930 recs, 713 with tokens) — **freq only, class untouched (hard-guarded)**;
      crosswalk re-emitted with `dcs_freq/rank/class_tag/attested_forms`. Lemma-level; token-level next.
- [x] **Phase 2 — DONE (2026-06-13).** Extracted MW (750 genuineroot) + Apte (3367) root entries
      (`scripts/extract_dict_roots.py`); aligned to the hub by SLP1+class (`scripts/dict_align.py`):
      **765/930 linked** (MW 569, Apte 697), 45 ambiguous → `crosswalk/alignment_review.json` +
      DECISIONS_NEEDED §5. `class` hard-guarded; `mw_id`/`apte_id`/senses in the crosswalk.
- [x] **Phase 3 — Wikisource ingest DONE (verb ch. IX–XV).** `fetch_whitney.py --full` → 497 §§
      (§§599–1095) in `src/whitney_sections.json`. Non-verb ch. I–VIII/XVI–XVIII deferred (no form→§ row lives there).
- [x] **Phase 4 — DONE (2026-06-13).** `build_form_section_edges.py` + `src/form_section_concordance.json`
      → **7315 root→form-category→§ edges** on 785/930 roots (aorist subtypes auto-detected), all validated
      against fetched §§; folded as `whitney_sections`, emitted as `root_section` table + `cito:isExplainedBy`.
- [x] **Phase 5 — DONE (2026-06-13).** Two-pane reader `reader/` (index.html · reader.css · reader.js)
      over `src/reader_data.json` (`scripts/build_reader_data.py`; 930 roots + 6079-form index). Click a
      word → root + class + **"Why this form?"** §§ (Wikisource links) + MW/Apte senses + collapsed
      corpus; √A/√B homonym chooser; graceful degradation. Verified in-browser (no console errors).

**Prior human-adjudication queues (still valid — fold into Phase 2 class/homonym curation),
fully specified in `REVIEWER_GUIDE.md` (EN/RU), in priority order:**

- [ ] **Queue A — 19 kept class additions** (`review_queue.json`). Each adds a *distinct*
      class (III/IV/VII/…) on a corpus prompt; confirm against Grammar §, then Zalizniak,
      else revert. Worked examples in the guide (krī +I, jñā +IV, 4 dā +III …).
- [ ] **Queue B — 12 suspicious high-frequency PPP** (`ppp_source_validation.md`). e.g.
      `han→ghata` (normal PPP is `hata`); validate against Grammar PPP §§ (§954–957).
- [ ] **Queue C — 76 malformed PPP** (`ppp_source_validation.md` → LIKELY_ERROR). Correct
      `janitos→janita`, `labdhva→labdha`, `bhugna 1→bhagna`, etc.
- [ ] **Queue D — 101 `exception` tags** (`src/grammar_refs.json`). Spot-check ~20; short-root
      false positives expected.
- [ ] **Queue E (optional)** — the 117 reverted I/VI pairs; only an accented source / Zalizniak
      can adjudicate. Not errors in Whitney — just cases the corpus can't speak to.

---

## 🚧 Current Work-In-Progress (WIP)

**✅ SHIPPED & LIVE (2026-06-14): vidyut paradigms (display+validation) + reader/app on GitHub Pages.**
PRs #6 (paradigms+validator+reader), #7 (paradigms in the v3 app), #8 (Pages deploy) all MERGED to main
(80121c1). **Site is live:** https://gasyoun.github.io/WhitneyRoots/ (v3 app) + /reader/ (reader).
Pages build_type switched legacy→workflow; `.github/workflows/pages.yml` deploys a CLEAN artifact
(index.html, index.css, v3_app.js, src/, reader/ — no scripts/scratch/docs on the public URL, verified
scratch/*→404) on every push to main. Reader nav link + back-link cross-navigate the two apps. Live
deploy verified: Reader link, buildParadigmSection, paradigms.json all served; 0 errors.

**vidyut-prakriya wired for paradigm DISPLAY + form-VALIDATION (2026-06-14) — its right role per the
pilot synthesis (NOT gaṇa attribution). Built → adversarially reviewed (`wf_d77e0d86-cac`, 5 dims,
22 confirmed findings) → ALL correctness findings fixed → re-verified in-browser. Final, clean state:**
- **Display:** `scripts/vidyut_paradigms.py` → `src/paradigms.json` (454 roots, 503 paradigms, 468
  corpus-attested, 519 KB, no BOM). Clean-root key = `result[0]` at the LAST it-lopa (rule **1.3.9**)
  BEFORE laṭ-insertion (3.2.123) — NOT at 3.2.123, which would keep the num-augment that nasalises idit
  roots (skud not skund). Exact-then-nasal-folded match (Y/N/R/M→n) recovers ~35 homorganic-nasal roots
  vidyut has (añj→anakti, kāṅkṣ, hiṃs, śaṃs, daṃś). **TWO GATES bind the right homonym:** (1) gaṇa ∈
  Whitney classes; (2) corroboration — where the corpus attests presents, the dhātu's generated PRESENT
  must match (present = gaṇa identity; PPP is shared so it can't bind a gaṇa). Two-pass: if any candidate's
  present is attested, only present-matched survive; else PPP-fallback; else gaṇa-only. This killed the
  homonym cross-contamination the review found: √jñā now IX *jānāti* only (no kill-root *jñāti*), √dā III
  *dadāti* only (no reap-root *dāti*), √kṛ VIII *karoti* only, √pā I *pibati* only, √budh aniṭ *buddha*
  only, √vid know/find no longer swap. 168 wrong-homonym intruders rejected, 70 gaṇa-divergent withheld.
- **Validation:** `scripts/vidyut_validate_ppp.py` → `crosswalk/ppp_validation.json` (advisory; never
  edits spine). **Post-refinement (2026-06-14, see Completed): 325 match, 13 mismatch, 33 fill_candidate,
  13 vidyut_only.** (Was 305/33/32/14.) Review fixes: `fill_candidate` label was INVERTED → relabeled;
  warnemyr `-` placeholder no longer false-flags 5 roots; `corpus_backs_vidyut` gated to genuine
  mismatches. Now honours warnemyr PPP DOUBLETS + vidyut CAUSATIVE kta, with per-item provenance
  (`match_basis`/`matched_against`/`matched_form`) so every verdict is revisable.
- **Shared `scripts/sanskrit_util.py`:** added `from_slp1()` (SLP1→IAST) and `form_key()` — a
  length-PRESERVING, nasal-folding, accent-stripping comparison key used by BOTH scripts (consolidated).
  THREE normalization bugs found by eyeball + a 4th by the review, all fixed in `form_key`: reader `norm()`
  collapses ā→a (length loss); U+0323 strip mangles ṣ/ṛ/ṭ; U+0301 *is* ś's diacritic (strip mangles ś,
  so only drop the acute when it sits on a VOWEL); accent on vocalic ṛ́/ṝ́ needs a walk-back past all
  combining marks. ś preserved, length preserved, anusvāra=homorganic-n folded.
- **Reader:** `reader/reader.js` + `reader.css` fetch `src/paradigms.json` (optional, non-fatal),
  render a collapsed "Paradigm — generated" section; non-attested (gaṇa-only) blocks marked "not in
  corpus"; overflow-x guard on the present table. Verified in-browser (√gam #170 full correct paradigm,
  √gach none, 0 console errors). Review confirmed the reader path graceful/XSS-safe/no-null-deref.
- **Shipped: PR #6 (paradigms + validator + reader)** MERGED to main (37f10e7).
- **v3-app integration (PR #7, 2026-06-14):** the generated paradigm now also shows in the MAIN
  state-driven app's root detail page (not just the standalone reader). `src/core/data.js` loads
  `src/paradigms.json` as an optional sidecar (DCS-freq pattern) → `item.paradigm`, joined id==whitney_no
  (454/454). `src/renderers/detail.js` adds a "Paradigm" section between Grammar and DCS Corpus (present
  3×3 table + other tenses 3sg·pl + kṛdantas, per gaṇa-homonym block, "not in corpus" tag on gaṇa-only
  blocks, createElement/textContent-safe, omitted when no paradigm). `index.css` paradigm styles; rebuilt
  `v3_app.js` via `node scripts/bundle.js`. Verified in v3 app in-browser (√gam full paradigm, correct
  section order, 0 console errors).


**Layer 1 (lexical crosswalk) shipped** — spine + FAIR emitters + decision dossier all landed this
session (see Completed). **Layer 2 (grammar) piloted** — Wikisource fetcher + 89 §§ + a verified
~32-row form→§ concordance.

**Layers 1 + 2 both shipped.** Each root now joins Grammar (class/PPP/paradigm + form→§ edges) ×
Corpus (DCS freq/forms) × Dictionaries (MW+Apte senses), keyed on `whitney_no`. FAIR crosswalk =
24k RDF triples.

**All five build phases shipped** (Layers 1+2 + reader; IAST + Devanagari input, 31k-form resolution).
Next actionable, all autonomous: (1) token-level corpus disambiguation (lemma freq shared across
homonyms); (2) ingest non-verb chapters I–VIII/XVI–XVIII for completeness; (3) deploy the reader
(GitHub Pages) or fold it into the v3 app as a route. Human-gated: `docs/DECISIONS_NEEDED.md`
(class/PPP + 45 dict-alignment ambiguities; Grammar §-evidence pre-pulled).

---

## 🧠 Dev Notes & Hypotheses

**⚠️ Critical methodology finding (the I/VI accent collapse).** The DCS corpus is **unaccented**.
Class I (`cárati`, guṇa root + accent on root) and class VI (`tudáti`, weak root + accent on `-á`)
have identical surface present-stems without accent. The verification script's patterns for I and
VI both match `root+a`, so it returned "I, VI" for nearly every thematic root, and the addition
step "completed the pair." This produced 117 spurious additions — **reverted**. The only reliable
non-accent tell is **root-vowel grade** where guṇa would change it (`viśáti` keeps `i` ⇒ VI; a
class-I form would raise it). Where guṇa changes nothing (`car`, `nam`), the corpus is silent.
Same trap for **IV vs passive** (both `root+ya`): `jñāyate`/`dhriyáte` are passives, not class IV.

**Short-root text contamination.** In `src/wg_text.txt`, root `as` matches the English word "as"
2482× (11× inside the VI chapter alone), `i` 598×. Auto `specific`/`exception` tags on short roots
can be false (Queue D).

**Grammar transliteration**: PDF uses `ç` (U+00E7) for IAST `ś`; builder normalises via
`to_grammar_conv(root)`. Confirmed by counts (ç:2665 vs ś:2).

**Chapter ranges**: Whitney's chapter numbering (I–IX) ≠ Hindu gaṇa (1–10). Builder uses Hindu
numbers matching `app_data.json`; char-position boundaries verified against chapter headers.

**Key Section A decisions** (Grammar is highest authority):
- `mṛ` (571/572): keep I. §731 = `mṛṇá` á-stem (I); §773 `mriyáte` is PASSIVE, not IV.
- `vadh` (705): keep I (nominal). Defective; present supplied by √han. §904a aorist, §1042g denom.
- `cūṣ` (234): keep IV. §240b phonological only; IV is from Whitney Roots.

**DCS `lemma.grammar` is itself lexicon metadata** (Böhtlingk/MW tradition), not attested-form
evidence. A disagreement there is lexicon-vs-lexicon, never corpus proof.

**Morphology ceiling for homonym splitting (2026-06-14, the pilot finding).** Of the 38 DCS-lumped
homonym groups, only **5 have gaṇa-disjoint homonyms** — the rest share a gaṇa, so *no* form
generator (vidyut) or per-token gaṇa attestation (ND-SWSMP) can separate them. Past this point,
splitting homonyms is a **lexical-semantic** task (gloss/MW sense match, as `token_disambiguate.py`
does on DCS's own lemma_ids), not a morphological one. Don't reach for a Pāṇinian generator to solve
what is a sense-disambiguation problem.

**Crosswalk source-of-truth = warnemyr.** The local `Whitney_roots_class-PP.txt` / `app_data.json`
classes are a lossy HTTrack scrape: `—` can mean *missing* (kḷp = really cl. I), and homonym classes
are **union-smeared** (2 as shows II but is I/IV; 1 kṛ shows VI but is I/II/V/VIII). Phase 0 re-derives
per-homonym class from warnemyr (rich paradigm + period tags V/B/S/E/C), keyed via the index
`{sense→URL}` map (cert-tolerant `curl -k`; never generate filenames — irregular: ś→z, ā→aa, ḷ→_l).
`src/wg_text.txt` (PDF-derived, ç=ś) is the prior §-text; §§ source is now **Wikisource** — reconcile
the two in Phase 3.

---

## ✅ Completed (Recent only)

**MW derivation oracle — 2026-06-26, branch `feat/mw-derivation-oracle`:**
- `scripts/build_mw_derivations.py` **consumes** csl-orig's pre-built `v02/mw/mw_etymology.tsv`
  (9,377 MW headwords with an identified root) instead of re-extracting, and joins it onto the
  Whitney spine by root surface SLP1 → new read-only sidecar `crosswalk/mw_derivations.json`
  (per-root MW productivity: count, deriv_type breakdown, sample headwords). 620 roots matched,
  7,654 unique MW derivations covered. Realizes the PROJECT_INTERLINKS "etymology.tsv → WhitneyRoots"
  feed. **Read-only:** touches no `app_data.json`, class field, PPP overlay, homonym split, or
  reviewed file. ➡️ Optional next: surface productivity in the reader UI (load like `dcs_freq`).

**PPP apparatus-bleed cleanup — 39 records — 2026-06-14 (Opus 4.8), branch `fix/ppp-apparatus-bleed`:**
- **Bug:** Whitney's scholarly *apparatus* (period/source markers `RV1 RV2 E1 C1 S1 B1 K R AA` + trailing
  period-digits, `?` uncertainty, `&`/space alternate-joiners, `= seq.` cross-refs, `adj`) bled into 39
  `lexicon[].ppp` arrays from the fixed-width source column. Broke `detail.js fold()` (false red ✗ chips:
  `fold("kupita RV1")` ≠ corpus `kupita`) and `analytics.js` length-counting (unsplit `&`/space alternates).
  DISTINCT from the 6 gloss-bleed records (ids 19,100,182,184,347,831) — those left untouched.
- **Fix:** `scripts/dcs/fix_ppp_apparatus_bleed.py` (mirrors `fix_ppp_gloss_bleed.py`) — surgical CRLF-safe
  text replacement, per-id anchored (shared ppp text in 371/372, 469/470/471, 708/709, 797/798/799),
  idempotent, baked-in verify. **Editorial decisions RESOLVED = PRESERVE** (user-confirmed): apparatus kept
  in 3 additive, consumer-safe fields — `ppp_attestation {form:[markers]}`, `ppp_uncertain [13 forms]`,
  `ppp_note {form:note}` (incl. id 74 dangling-`=` human flag). `&` always split; bare space split only for
  the confirmed variant pair 747 `vrdha`/`brdha`; id 649 OCR dup `rises` dropped.
- **Verified:** scan→0 apparatus records; JSON valid, BOM-free, CRLF, 935 entries, no trailing newline;
  2nd run = no-op; replicated `detail.js` fold+match vs `src/dcs_freq.json` → the false ✗ chips flip green
  where corpus attests (√kup `kupita`, √tim `timita`, √van `vata` all ✓), residual ✗ are legitimate
  non-attestation. Audit: `docs/ppp_apparatus_bleed_audit.json`. PR-only (data-only scope); decisions
  recorded in `PPP_APPARATUS_BLEED_WORKLIST.md` §4 (NOT `DECISIONS_NEEDED.md` — that file had unrelated
  uncommitted §3e work in the tree, left untouched). `app_data.json` is data (not bundled) → no `v3_app.js` rebuild.

**PPP validation — doublet + causative refinement (Option A) — 2026-06-14 (Opus):**
- **Root cause:** the spine's `ppp` stored only warnemyr's FIRST PPP (from the HTML page's `PPP :`
  line), so a root whose SECOND recorded form is the one vidyut generates (√gup `gupita, gupta`;
  vidyut `gupta`) was a FALSE `mismatch`. The doublet lives only in `Whitney_roots_class-PP.txt`
  (col 4, ASCII-romanised).
- **`scripts/vidyut_validate_ppp.py`:** now parses ALL comma-separated forms from the numbered source
  (`load_warnemyr_forms` + `merge_forms`, accented spine form kept first as `warnemyr_ppp` for
  back-compat → new `warnemyr_ppp_forms`); a root is `match` if ANY form hits vidyut **primary OR
  causative** kta, or the DCS top form (comparison via length-preserving `form_key`, NOT reader
  `norm()`). New per-item provenance: `match_basis` (spine/doublet/source_alt/corpus_fill),
  `matched_against` (vidyut/vidyut_caus/dcs), `matched_form` — so any class of flip is revisable.
- **`scripts/vidyut_paradigms.py`:** added CAUSATIVE (ṇic) kta generation via `d.with_sanadi([Ric])`
  → new `krt.ppp_caus` per paradigm (√gam→gamita, √śam→śamita, √gup→gopita). Regenerated
  `src/paradigms.json` — verified STRICTLY ADDITIVE (0 churn in 503 paradigms, ppp_caus on all 503).
  Both display panels (`detail.js` KRT_ORDER, `reader.js`) use allowlists → ignore the new key, no UI
  change, no rebundle needed.
- **Effect (Option A, user-chosen):** mismatches **33 → 13** (20 resolved). 18 via doublet/dcs (6 comma
  doublets gup/gras/dā/du/vij/hras; 2 source_alt ji/vid; 10 corpus-agreement via `dcs`), +2 via causative
  (dhāv `dhāvita`, śam `śamitá`); vañc moved vidyut_only→fill. 0 regressions. Of the panel's 13
  `corpus_backs_vidyut`, **8 auto-resolved, 5 remain** (iṣ/kṣubh/piś/mṛ/hā — blocked only by the
  ASCII-romanised source column, which `form_key` correctly keeps distinct from ṣ/ś/ṛ).
- **⚠️ Flagged for human review (in `docs/DECISIONS_NEEDED.md` §3d):** ji #259 & vid #729 (`source_alt`:
  numbered source lists a form ≠ HTML page, overrides the §3a homonym KEEP); **dā #350** (`datta` is the
  √dā "give" PPP on the "divide" entry — likely an эталонный numbering smear, cf. §3c).
- **Docs:** `scripts/build_decisions_doc.py` updated (PPP_NOTES note + PPP_REVIEW + §3d block); regenerated
  `docs/DECISIONS_NEEDED.md` (§3a 8→3, §3b 5→2, new §3d with the 8 auto-resolved + revise-how guidance).
- **SHIPPED:** code + paradigms + Caus. PPP display row (`detail.js`/`reader.js`, rebundled `v3_app.js`),
  verified in-browser, committed `bc033a5` and pushed to main (Pages redeploy).
- **Whitney-Grammar verification vs live Wikisource (2026-06-14, wf 16 agents, 0 over-claims):** every
  grammar-RULE aspect is confirmed VERBATIM against en.wikisource.org/wiki/Sanskrit_Grammar_(Whitney) —
  the ta/ita & ná doublets (§956b names gup/kṣubh/gras/piś "both forms", iṣ send; §957c "vigna beside
  vikta"; §957a "dā divide → diná, also dita & -tta" NOT datta, "dū burn also duta"; §957d "vinna √vid
  find: also vittá"; §955a śānta √çam be quiet), the homonym splits, and the causative PPP in -ita (§1051a
  "the passive participle, as dhārita"; §1051b īritá/vāsita/çrāvitá). VINDICATES the §3d flags (dā #350,
  vid #729). The 5 surviving mismatches (kṣubh/piś/iṣ/mṛ/hā) are ALL Whitney-confirmed real forms → the
  residual flag is purely an ASCII-romanisation artifact, 0 true disagreements. ⚠️ The per-root Roots
  list — Whitney's 1885 supplement *Roots, Verb-Forms, and Primary Derivatives* (warnemyr's source) — is
  NOT on Wikisource in any namespace (3 API queries + TOC; Grammar's own Appendix = bhū/kṛ synopsis only;
  supplement lives on archive.org/HathiTrust). So only the RULES are "at Wikisource", not the per-root data.
  Nuance: §954b assigns iṣṭá to √yaj (√iṣ-desire→iṣṭá is the standard analysis but not Whitney's §954 example).
- **§3e added** (`build_decisions_doc.py` + regenerated `DECISIONS_NEEDED.md`): the 10 corpus-frequency
  (`matched_against==dcs`) matches are ATTESTATION, not a grammar rule → Whitney can't adjudicate → LEFT
  FOR LATER (human/Zalizniak or the 1885 supplement). Doc-only follow-up commit.
- **kṣubh + piś diacritic restoration (2026-06-14):** `WHITNEY_RESTORE` map in `vidyut_validate_ppp.py`
  diacritic-restores ONLY the two source forms Whitney confirms as single-root doublets (kṣubh
  `ksubhita`→`kṣubhita` §956b.4; piś `pisita`→`piśita` §956b.5) so they match vidyut. The 3 homonym KEEPs
  (mṛ/iṣ/hā, §3a) are deliberately NOT restored — their alternant belongs to a different root, so the
  ASCII-folding correctly keeps them flagged. **mismatch 13→11, match 325→327, corpus_backs_vidyut 5→3.**
  §3b now empty (both editorial doublets resolved), §3a=3, §3d=10/13. 0 regressions. Landed off
  origin/main via worktree (external actor was mid-flight on main / feature branches).

**PPP column-bleed data fix — 2026-06-14 (Opus):**
- Fixed the gloss-bleed bug where the source PPP column (`Whitney_roots_class-PP.txt`) carried
  trailing English gloss / HTML junk that got comma-split into bogus `ppp` array elements in
  `src/app_data.json`. Audited all 935 records (44 had spaced `ppp` elements; cross-checked
  standalone elements against the English meaning vocabulary — no additional cases). Exactly
  **6 genuine gloss-bleeds**, all corrected by `scripts/dcs/fix_ppp_gloss_bleed.py` (idempotent,
  CRLF/BOM-preserving, self-verifying): **19** av `[uta favour,like,delight in]`→`[uta]`,
  **100** kuṣ→`[kusita]`, **182** guṇṭh→`[gunthita]`, **347** das→`[dasta]`, **831** san→`[sata]`,
  and **184** guph `[]`→`[gumphita]` (form had been lost entirely to HTTrack/Copyright junk).
- The other ~38 spaced `ppp` elements are legitimate source apparatus (refs RV1/R/K/E1/C1/S1,
  uncertainty `?`, `=`, footnote digits, alt-forms `turta & turna` / `vrdha brdha`) — left intact;
  normalising those is a separate editorial question, not this bug.
- Parser audit (conclusion): the only in-repo parser, `scripts/parse_whitney.js`, is **stale and
  not the producer** of the live `src/app_data.json` — it writes to a non-existent root
  `../app_data.json` with a different schema (`ppp` as a string, `id:"root_N"`), and its
  `split('\n')` + `\s{2,}` regex doesn't even match the CRLF source (trailing `\r` breaks `(.*)$`).
  Live data is maintained via the `scripts/dcs/*` apply/fix scripts, so the gloss-bleed is corrected
  there, per-record. (A first attempt added a defensive `stripMirrorJunk()` to that parser; a code
  review showed it was inert — wrong column + dead output path — so it was reverted.)
  JSON re-verified: valid, BOM-free, CRLF preserved, 935 entries.
- Follow-up SCOPED + OPENED as its own task (2026-06-14): the apparatus bleed = exactly **39**
  records (`scripts/dcs/scan_ppp_apparatus.py` enumerates them) carrying source apparatus inside `ppp`
  (e.g. `kupita RV1`, `cite ?`, `trsta ? adj`, `turta & turna`, `rise rises`); these feed `detail.js`'s
  corpus-fold (false ✗ chips) and `analytics.js` ppp-count scoring. Pre-existing, NOT gloss-bleed.
  All 39 classified + adversarially verified (workflow `wf_75c62a06-764`, 13 agents, all `agree:true`) →
  full spec with correction table + 5 editorial decisions (preserve-vs-drop markers / `?` / alt-split /
  `= seq.`/`adj` / `rise rises`) at `docs/PPP_APPARATUS_BLEED_WORKLIST.md`. Opened as background task
  (chip; mirror `fix_ppp_gloss_bleed.py`, additive metadata fields, PR-only). Editorial decisions still
  need a human call before the bulk edit.

**Phase 0 finish + Layer-1 crosswalk + Layer-2 pilot — 2026-06-13 (Opus):**
- **Phase 0:** retargeted `parse_warnemyr.py` at the full local mirror `1885/`; built
  `root_spine.json` (939 recs / 930 keyed / 0 dup / 0 BOM) + 23-flag `audit.md`. Added a
  `class_uncertain` field so warnemyr's `ROMAN ?` (uncertain gaṇa; 35 roots, incl. `han` +VI) is
  captured but never pollutes asserted `class`.
- **FAIR emitters:** `scripts/emit_crosswalk.py` → `crosswalk/{roots.csv, root_class.csv, roots.sqlite,
  roots.ttl, roots.csv-metadata.json}` (OntoLex-Lemon + thin `wr:` ns; rdflib-validated).
- **Phase 1 corpus fold:** `scripts/fold_corpus.py` → spine `corpus` key (713/930 with DCS tokens;
  `class` hard-guarded untouched); crosswalk re-emitted with corpus columns (14.6k triples).
- **Phase 2 dict arm:** `extract_dict_roots.py` (MW 750 `genuineroot` + Apte 3367 `{cNc}` roots) +
  `dict_align.py` (hub-side SLP1+class, class-guarded) → **765/930 linked** (MW 569, Apte 697); 45
  ambiguous → `crosswalk/{root_alignment.csv, alignment_review.json}` + dossier §5. Homonym split
  verified: `as`/1 (II, “be”) → MW L20353 vs `as`/2 (I/IV, “throw”) → MW L20354.
- **Phase 3 Wikisource `--full`:** 497 §§ (§§599–1095, ch. IX–XV) in `src/whitney_sections.json`.
- **Phase 4 grammar cross-link:** `src/form_section_concordance.json` (~40 categories) +
  `build_form_section_edges.py` → **7315 root→form-category→§ edges** (785/930 roots; gaṇa from class,
  aorist subtypes parsed from paradigm text; class-guarded), emitted as `crosswalk/root_section_edges.csv`
  + SQLite `root_section` + RDF `cito:isExplainedBy`. Crosswalk now **24k triples**. √vac → both present
  classes + perfect + aorist(a,redup) + futures + PPP/inf/ger + caus/des/int, each a Wikisource §-link.
- **Phase 5 reader:** `scripts/build_reader_data.py` → `src/reader_data.json` (930 roots + 6079-form
  index, 1.6 MB); standalone two-pane `reader/` (vanilla JS, no build). Token → root → "Why this form?"
  §§ + senses + corpus; homonym chooser (gacchati → √gach / √gam·I-II) + graceful degradation.
  Verified via preview (port 8733, `reader` in `GitHub/.claude/launch.json`); 0 console errors.
- **Reader resolution enrichment:** `build_reader_data.py` now scans `dcs_full.sqlite` (upos=VERB) for
  every attested surface form of each Whitney-linked lemma → form index **6079 → 31,451 keys** (2.0 MB).
  Finite/perfect/aorist/imperfect forms now resolve (avocat→vac, babhūva→bhū, cakāra→kṛ, abravīt→brū);
  candidates ranked by DCS freq so mis-tags sort last; multi-word/bound forms filtered. Verified in-browser.
- **Reader Devanagari input:** `reader.js` gains a Devanagari→IAST transliterator (conjunct/virama +
  inherent-vowel handling) feeding the same folded index; danda ।/॥ become separators; IAST + देवनागरी
  example buttons. देवनागरी passage उवाच→√vac, कृत्वा/गच्छति→choosers, nouns unresolved — verified, 0 errors.
- **/code-review fixes (xhigh, 2026-06-13):** fixed the confirmed-correctness findings — (1) form→§
  categories **26→33 emitted**: detection moved to `parse_warnemyr.detect_forms()` on FULL paradigm text
  (kills the 240-char truncation that dropped kṛ's aor_redup) + reliably-markable categories added, NOT
  the 8 warnemyr-doesn't-flag ones (precative/conditional/pluperfect/…); (2) sa-aorist token now
  space-insensitive → `aor_sa` emitted; (3) **Devanagari anusvāra** folds to homorganic nasal (कांक्षति→√kāṅkṣ
  resolves) via a shared nasal-folding `norm()` in new `scripts/sanskrit_util.py` (retires the 4× norm/
  to_slp1/to_roman copies); (4) MW class regex anchored to the class clause (multi-class **108→55**, citation
  digits dropped); (5) `root_slp1` stored on the spine → dict_align/extract read it (no roots.csv ordering
  cycle); + iv_pair label, period_tags-over-prose, dict 1→N-homonym `slp1-shared` flag. Pipeline re-run
  (10001 edges, 26.6k triples); reader re-verified in-browser.
- **Adversarial verify workflow (5 skeptics) + fix-of-fixes:** the workflow confirmed 3 fixes SOUND
  (nasal-fold, root_slp1/decoupling, edge-validation) and caught 3 regressions in the others — all fixed:
  (a) removed fabricated `adverbial_gerund` (~167 roots) — warnemyr '2 Abs' is the **-ya gerund** (§§989–994),
  not the -am §995; (b) added `precative` (§§921–926) + `passive_aorist` (§843) — warnemyr DOES write
  'Precative :' / 'Ao/Ps/3/sg :'; (c) MW gaṇa now read from the **authoritative `cp="…"` attribute**
  (738/750 roots) — fixes the 6 residual person/number phantoms AND the pre-existing 189-root under-capture
  (fD→[2,4,5,6,7], kram→[1,4]). Final: 34/41 categories (7 genuinely unmarked), 9878 edges, 26.5k triples;
  reader 0 console errors.
- **2nd /code-review (xhigh) + cleanup/guards/two-tier:** fresh review of the fix commits found no new
  active crash but 1 precision issue + latents. Applied: **two-tier reader lookup** — exact `form_index`
  (`am`→am#13, `an`→an#12 now DISTINCT) + a `fold_alias` nasal-fold FALLBACK (कांक्षति→kāṅkṣ recall kept),
  so the nasal fold no longer over-merges roots on the exact key (`norm` exact / `nfold` recall split in
  sanskrit_util + reader.js). Guards for missing `root_slp1` (dict_align assert / extract warn); excluded
  Derivatives prose from the precative/passive_aorist blob; removed dead `AOR`/`import re`/`unicodedata`,
  orphaned concordance `aorist_subtype`, simplified gerund regex; `SELECT DISTINCT` on the DCS scan; fixed
  stale docstrings + documented the `slp1-shared` accepted-but-flagged tier. Pipeline re-run; reader 0 errors.
- **Token-level corpus disambiguation (2026-06-14):** `scripts/token_disambiguate.py` recovers
  per-homonym DCS freq from **DCS's own verb `lemma_id` separation** (each lemma_id mapped to its
  Whitney homonym by **GLOSS** — DCS `meanings` ↔ warnemyr gloss, primary — then gaṇa, fallback) — NOT
  stem-matching, which mis-split √as (be↔throw 909/21323 backwards). Conservative guard: ≥2 verb
  lemma_ids mapping 1:1 to ≥2 distinct homonyms (cov≥0.55). **26 reliable splits** (vid know 9391/find
  1923; mṛ die 2601/crush 14; as be 35466/throw 287; kṛ make 40555/scatter 211; pā/iṣ/dhā/bhuj/vas/vṛ/
  kṣi/naś/…); 46 left shared (38 DCS-lumped under one lemma_id + ambiguous). Folded as
  `corpus.dcs_freq_token`; reader shows "N (this sense) · M for the lemma". Audit: `crosswalk/token_attribution.json`.
  The gloss-mapping came from a tool-survey workflow (DCS per-token LemmaId is the authoritative homonym
  key); the 38 lumped groups would need vidyut-prakriya (MIT form-gen) or the ND-SWSMP DCS↔Heritage
  alignment dataset to split further — both ready, see the survey result.
- **Pilots: vidyut-prakriya vs ND-SWSMP (2026-06-14) — both complete; recommendation = HOLD.**
  Local join `scratch/ndswsmp/sh_whitney_crosswalk_prototype.json` settles the gating question:
  **only 5 of 38 DCS-lumped groups are gaṇa-distinct** (splittable by class at all) —
  `pat`(3123: 1→#440 fall/4→#441 rule), `khād`(369), `dīv`(110), `luṭh`(26), `akṣ`(4); **3,632 tokens,
  86% in `pat`**. The other **33 share a gaṇa** between homonyms (`gam` 1/2, `ji`, `i`, `stu`, `vah`,
  `śam`, `yam`…) → no morphological signal from any tool separates them; they need sense/gloss
  adjudication (which `token_disambiguate.py` already does for the 26 reliable). **So neither pilot
  raises coverage beyond ~5 groups.** Tool fit differs: **vidyut** (MIT, no download, deterministic)
  *generates* paradigms → only routes present-system tokens where stems diverge, and **fails on `pat`**
  (cl.4 'rule' #441 absent from its Dhātupāṭha → can't attribute a single 'rule' token). **ND-SWSMP**
  (CC-BY-4.0, flaky Drive fetch; `wsmp_dev.json` never landed) *attests* per-token `[gaṇa]` across all
  tense-systems → would split `pat`. **Recommendation:** ship current 26 splits as the answer; if the
  5 are ever wanted, prefer ND-SWSMP **vendored once** (not a live dependency); keep vidyut on the shelf
  for paradigm display / form-validation, not gaṇa attribution. **Morphology is mined out — residual
  homonym disambiguation is now lexical-semantic, not morphological.** Confirm before building either.
- **Decision dossier:** `scripts/build_decisions_doc.py` → `docs/DECISIONS_NEEDED.md` — Grammar-§
  evidence pre-pulled for the 19 Queue-A adds, the 3 urgent I/VI-on-empty (`pṛṇ/mṛṇ/sphur`: +VI
  Grammar-supported §753a/§756, +I not), and the 23 Phase-0 flags.
- **Code review (subagent):** the 11 unpushed commits verified safe-to-push — revert correct (120
  restored to baseline, 0 over/under-revert), PPP idempotent, no BOM.
- **Layer-2 pilot (subagent):** `scripts/wikisource/fetch_whitney.py` + `src/whitney_sections.json`
  (89 §§) + verified `docs/form_to_section_concordance.md` (~32 rows).
- ⚠️ Observed an **external edit** to `CHANGELOG.md` mid-session (Unreleased → `[1.0.0] 2026-06-13`);
  left it untouched — not mine.

**Crosswalk design + Phase 0 start — 2026-06-12 (Opus):**
- `DESIGN.md` — full two-layer crosswalk spec (data model, homonym alignment, LOD/§, reader UX, build order).
- warnemyr root index harvested → `scratch/phase0/wn_index.tsv` (937 pages; 144 homonym-split, 150 grouped).
- `scratch/phase0/parse_warnemyr.py` built + validated on a 9-page pilot; class-gap audit working
  (distinguishes GAP / SMEAR / true-defective). Full harvest running.

**Phase 8 (Critical review + revert) — Opus pass, 2026-06-10:**
- Empirically confirmed the I/VI accent-collapse and short-root contamination (`scratch_verify_method.py`).
- Audited all class changes vs pure Whitney Roots (`18b51b1`): 139 additions, 0 removals, 2 invalid labels.
- **Reverted 120 unsound additions** (`scripts/dcs/revert_collapse_additions.py`):
  117 I/VI collapse + 2 invalid `IV|PASS` + 1 Section-B `yam +VI`. Kept 19 distinct-class
  additions, flagged in `review_queue.json`.
- Wrote `REVIEWER_GUIDE.md` (EN/RU), `CHANGELOG.md`; updated this file.

**Phase 7 (PPP source validation & corrections):**
- Validated 208 PPP entries vs 381K corpus forms: 76 LIKELY_ERROR, 12 SUSPICIOUS, 50 ATTESTED, 88 PLAUSIBLE_GAP.
- Applied: `dā` 349/350/351 `tta`→`dātta`; removed 12 impossible forms (`-tave`/`-tos`/`-dhyai`).

**Phases 1–6 (superseded data edits, tooling retained):**
- Grammar §-extraction (`grammar_ref_builder.py`) → `Whitney_Grammar_Citations.md`,
  `src/grammar_refs.json`, `grammar_ref` field, `src/wg_text.txt` cache.
- DCS corpus verification (`corpus_verify_classes.py`) → `corpus_class_verdicts.json`
  (136 AGREE, 42 AGREE_AMBIG, 231 CONFLICT, 146 UNCLEAR, 302 NOT_ATTESTED).
- Section B + conflict additions (131) and PPP review tooling — **the 131 class additions were
  the subject of the Phase 8 revert**; the analysis docs (`detailed_conflict_triage.md`,
  `candidates_for_addition.md`) are retained for history only and marked superseded.
